<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Carrito de Compras</title>
</head>
<body>
    <h1>Carrito de Compras</h1>
    <h3>Cart ID: {{cart._id}}</h3>
    <div class="products-container">
        {{#each cart.products}}
        <div class="product-card">
            <img src="{{#if this.product.thumbnails.[0]}}{{this.product.thumbnails.[0]}}{{else}}https://via.placeholder.com/250{{/if}}" alt="{{this.product.title}}">
            <h2>{{this.product.title}}</h2>
            <p>{{this.product.description}}</p>
            <p class="price">$ {{this.product.price}}</p>
            <p class="quantity">Cantidad: {{this.quantity}}</p>
        </div>
        {{/each}}
    </div>
</body>
</html>
